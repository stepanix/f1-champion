import { Circuit } from 'src/app/core/models/circuit.model';
import { locationStub } from './location.stub';

export const circuitStub: Circuit = {
  circuitId: 'MockId',
  url: 'mock url',
  circuitName: 'mock circuit name',
  Location: locationStub
};
